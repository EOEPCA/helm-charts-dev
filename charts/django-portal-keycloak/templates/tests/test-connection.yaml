apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "django-portal-keycloak.fullname" . }}-test-connection"
  labels:
    {{- include "django-portal-keycloak.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "django-portal-keycloak.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never