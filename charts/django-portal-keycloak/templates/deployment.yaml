apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "django-portal-keycloak.name" . }}
  labels:
    {{- include "django-portal-keycloak.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "django-portal-keycloak.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "django-portal-keycloak.name" . }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: {{ .Chart.Name }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        envFrom:
        - configMapRef:
            name: {{ include "django-portal-keycloak.name" . }}
        - secretRef:
            name: {{ include "django-portal-keycloak.name" . }}
        ports:
        - name: http
          containerPort: 80
          protocol: TCP