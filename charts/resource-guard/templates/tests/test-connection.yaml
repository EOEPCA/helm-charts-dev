apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "resource-guard.fullname" . }}-test-connection"
  labels:
    {{- include "resource-guard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "resource-guard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
